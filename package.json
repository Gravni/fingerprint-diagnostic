{
  "name": "fingerprint-diagnostic",
  "version": "4.3.0",
  "description": "Browser-fingerprint diagnostic core — cross-realm capture + network JA4/JA3, with open-source engine controls.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "bash scripts/fp-test.sh",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
