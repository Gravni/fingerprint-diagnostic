import type { ExecutableTreeManifest } from "./fp-provenance.mjs";
export const FP_EXECUTABLE_TREE: ExecutableTreeManifest;

/** Closed manifest of the browser-collector build inputs (template + encoder + renderer). */
export const FP_COLLECTOR_INPUTS: readonly string[];
